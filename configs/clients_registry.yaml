# Registry of clients for batch Deep Dive runs.
#
# Two formats supported:
#
# Multi-vehicle (recommended):
#   client_name:
#     output_subdir: "ClientName"
#     vehicles:
#       vehicle_name:
#         specs_path: "client_vehicle.yaml"
#
# Legacy single-vehicle (backward compat):
#   client_name:
#     specs_path: "client.yaml"
#     output_subdir: "ClientName"
#
# To run all vehicles for a client:   clients=["bradesco"]
# To run one specific vehicle:        clients=["bradesco_tiktok"]

clients:

  stellantis:
    output_subdir: "Stellantis"
    vehicles:
      fastback:
        specs_path: "stellantis_fastback.yaml"
        output_subdir: "Stellantis_Fastback"  # ambos vehicle: tiktok -> precisa distinguir a pasta de output
      pulse:
        specs_path: "stellantis_pulse.yaml"
        output_subdir: "Stellantis_Pulse"
