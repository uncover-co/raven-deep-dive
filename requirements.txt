# ── Uncover internal packages ────────────────────────────────────────────────
# Instalar via git (acesso interno necessário):
git+ssh://git@github.com/uncover-co/mmmverse.git
git+ssh://git@github.com/uncover-co/mammoth.git
git+ssh://git@github.com/uncover-co/pytoolkit-webserver-sdk.git
git+ssh://git@github.com/uncover-co/uncoverpy-deploy.git
git+ssh://git@github.com/uncover-co/autommm_mle.git

prophetverse>=0.12.0

# ── AWS SDK ───────────────────────────────────────────────────────────────
boto3

# ── PyStan ───────────────────────────────────────
pystan>=3.0.0

# ── JAX stack ────────────────────────────────────────────────────────────────
jax>=0.4.0
jaxlib>=0.4.0
numpyro>=0.15.0
optax>=0.2.0

# ── MLflow ───────────────────────────────────────────────────────────────────
mlflow>=2.10.0

# ── Google Meridian  ────────
google-meridian>=1.2.0

# ── Data ─────────────────────────────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.26.0

# ── Visualização ─────────────────────────────────────────────────────────────
plotly>=5.24.0

# ── Configuração ─────────────────────────────────────────────────────────────
pyyaml>=6.0

# ── Notebooks ────────────────────────────────────────────────────────────────
ipykernel>=6.0.0
nbformat>=5.9.0

# ── Testes ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-mock>=3.10.0